
  # FPM SA Prototype for IT (copie)

  This is a code bundle for FPM SA Prototype for IT (copie). The original project is available at https://www.figma.com/design/Ss0bekNcGcA9RA80hZAxhq/FPM-SA-Prototype-for-IT--copie-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  